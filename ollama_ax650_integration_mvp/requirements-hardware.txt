# Hardware Integration Dependencies
# Install these on Raspberry Pi with AX650 hardware

# Core SDK (install wheel from GitHub releases)
# axengine==0.1.3  # Install via: pip install axengine-0.1.3-py3-none-any.whl

# Data types for bfloat16 support (required for LLM models)
ml-dtypes>=0.1.0

# Numerical operations
numpy>=1.22.0

# Tokenizer support (for text generation)
transformers>=4.30.0

# Image processing (if using vision models)
Pillow>=9.0.0
opencv-python-headless>=4.5.0

# HTTP client (for Ollama adapter)
requests>=2.28.0

# Web server (already in requirements.txt, listed here for completeness)
Flask>=2.3.0
